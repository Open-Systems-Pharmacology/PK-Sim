name: Build and Test 13.1

on:
  pull_request:
    branches:
    - V13.1
  push:
    branches:
    - V13.1

permissions:
  contents: read
  packages: read
  
jobs:

  build:
    uses: Open-Systems-Pharmacology/Workflows/.github/workflows/test-csharp.yml@main
    secrets: inherit
    with:
      temp-version: "13.1.9999"
      build-arguments: "/p:ExcludeDesigner=true"
